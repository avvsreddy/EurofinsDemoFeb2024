
@model List<KnowledgeHubPortal.WebApplication.Models.Domain.Entities.Category>




@{
    ViewBag.Title = "Index";
}
<div class="row">
    <div class="col-9">
        <h2>Category List <span class="badge bg-secondary">@Model.Count</span></h2>

        @if (TempData["Message"] != null)
        {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <strong>@TempData["Message"]</strong> 
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        }

    </div>
    <div class="col-3">
        <a href="/Categories/Create" class="btn btn-success">Add New Category</a>
    </div>
</div>
    @*<table class="table table-bordered table-hover" >
            <tr>
                <td>Category ID</td>
                <td>Name</td>
                <td>Description</td>
            </tr>
            @foreach (var item in Model)
            {


            <tr>
                <td>@item.CategoryID</td</div>
                <td>@item.CategoryName</td>
                <td>@item.CategoryDescription</td>
            </tr>
            }

        </table>*@

    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 col-sm-1 m-2">
                <div class="card" style="width: 20rem;">
                    <div class="card-body">
                        <h5 class="card-title">@item.CategoryName</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@item.CategoryDescription</h6>
                    </div>
                    <div class="card-footer">
                        <div class="row">
                            <div class="col-8">
                                <a class="btn btn-primary" href="/Categories/Edit/@item.CategoryID">Edit</a>
                            </div>
                            <div class="col">
                                <a class="btn btn-danger align-content-end" href="/Categories/Delete/@item.CategoryID">Delete</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>






